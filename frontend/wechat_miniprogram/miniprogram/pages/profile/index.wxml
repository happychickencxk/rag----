<view class="page profile-page">
  <view class="user-card">
    <view class="avatar">张</view>
    <view>
      <view class="user-name">{{user.name}}</view>
      <view class="user-role">{{user.department}} · {{user.role}}</view>
    </view>
  </view>

  <view class="stats">
    <view class="stat-item">
      <view class="stat-value">48</view>
      <view class="stat-label">历史问答</view>
    </view>
    <view class="stat-item">
      <view class="stat-value">3</view>
      <view class="stat-label">反馈记录</view>
    </view>
    <view class="stat-item">
      <view class="stat-value">2</view>
      <view class="stat-label">常用知识库</view>
    </view>
  </view>

  <view class="section card">
    <view class="section-title">我的知识库权限</view>
    <view class="permission-row" wx:for="{{knowledgeBases}}" wx:key="id">
      <text class="{{item.permission === 'denied' ? 'muted' : ''}}">{{item.name}}</text>
      <text class="permission {{item.permission}}">{{item.permissionText}}</text>
    </view>
  </view>

  <view class="section card">
    <button class="setting-cell" bindtap="clearHistory">清空历史会话</button>
    <button class="setting-cell">消息通知</button>
    <button class="setting-cell" bindtap="openStates">异常状态预览</button>
    <button class="setting-cell">关于系统</button>
  </view>

  <button class="logout" bindtap="logout">退出登录</button>
</view>
